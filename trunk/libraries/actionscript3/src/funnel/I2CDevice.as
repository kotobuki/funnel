package funnel{	import funnel.IOModule;	/**	 * This is the class to express I2C devices	 *	 */	public class I2CDevice {		protected var WRITE:uint = 0;		protected var READ:uint = 1;		protected var READ_CONTINUOUS:uint = 2;		protected var STOP_READING:uint = 3;		public var address:uint;		protected var _io:IOModule;		public function I2CDevice(ioModule:IOModule, slaveAddress:uint) {			_io = ioModule;			address = slaveAddress;			_io.addSysexListener(this);		}		public function update():void {			// To be implemented in sublasses		}		public function handleSysex(command:uint, data:Array) {			// To be implemented in sublasses		}	}}